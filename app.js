const fullUserName = 'Вася aka Terminator Пупкин';
const userName = fullUserName.slice(0, userName.indexOf(' '));
const userSurname = fullUserName.slice(userName.lastIndexOf(' ') + 1);
console.log(name, surname);